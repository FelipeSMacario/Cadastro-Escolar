spring:
  datasource:
    username: root
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/sistemaescolar
    password: root
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8082
          #jwk-set-uri: http://localhost:8082/oauth2/jwks